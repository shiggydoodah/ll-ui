# CLAUDE.md

See [AGENTS.md](./AGENTS.md) — this repo keeps a single set of agent
instructions there. In particular: **do not update `CHANGELOG.md`.** ll-ui is
pre-release with no consumers, so the commit history is the record until the
first public version ships.
